# from django.urls import path
# from .views import UserProfileView, ActivityLogView

# urlpatterns = [
#     path('profile/', UserProfileView.as_view(), name='user-profile'),
#     path('activity/', ActivityLogView.as_view(), name='user-activity'),
# ]
